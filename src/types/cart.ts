export interface CartItem {
    id: number;
    name: string;
    image: string;
    price: number;
    quantity: number;
}

export interface AddToCartItem {
    id: number;
    name: string;
    image: string;
    price: number;
}